TODO
####

- Allow the publisher to pick a color when creating a poll
- Provide defaut color for each poll
- Do not store sensible information in browser's localStorage
- Discriminate users with publish credentials and other users (?)
- Limit publisher name (username) characters (how many?)
- Username can not have special characters, only alpha numaric values