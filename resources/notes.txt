TODO
####

- Allow the publisher to pick a color when creating a poll
- Provide defaut color for each poll
- Do not store sensible information in browser's localStorage
- Discriminate users with publish credentials and other users (?)
- Limit publisher name (username) characters (how many?)
- Username can not have special characters, only alpha numaric values
- Check the quick result bar is displaying the right proportions
- Add My favorites in tags filter section
- Add My polls in Publishers section
- Tags format does not accept space character
- The Cancel button on the new poll form will clear the form. Only external click or click on the cross will close the modal without clearing the content